# Temporary Python Prediction Trace Output

This directory exists as a documented scratch area for ad hoc Python prediction-trace exports during debugging.

Unlike the committed fixture directories under `tests/fixtures/`, this folder is for short-lived working output and
investigation-specific trace runs.

See [`docs/fixtures.md`](../docs/fixtures.md) for its intended role, promotion rules, and the distinction between
temporary debugging output and committed reference data.
