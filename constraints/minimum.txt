# Minimum supported dependency set for release-science compatibility checks.
# Updated: 2026-07-25
#
# Use this for the minimum-dependency CI lane, not for normal reproducible CI.
# Normal CI uses constraints/ci.txt. This file pins declared lower bounds for
# runtime and test dependencies that are installed by the minimum lane. The CI
# lane uses Python 3.11, the lowest supported interpreter.

numpy==1.24.0
pandas==2.0.0
scipy==1.10.0
scikit-learn==1.6.0

hypothesis==6.0.0
pytest==8.0.0

setuptools==77.0.3
wheel==0.45.1
