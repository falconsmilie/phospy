# R Reference Fixtures

This directory contains committed R reference fixtures for the small synthetic parity path.

Generate them with:

```bash
Rscript scripts/generate_r_fixtures.R
```

See [`docs/fixtures.md`](../../../docs/fixtures.md) for file descriptions, generation options, and maintenance rules.
