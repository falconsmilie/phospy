# Python Prediction Trace Fixtures

This directory contains committed Python-side prediction traces exported from the native predictor for seam-level
comparison with the R traces.

See [`docs/fixtures.md`](../../../../docs/fixtures.md) for generation commands, file descriptions, and maintenance
rules.