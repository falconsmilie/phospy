# Validation Ownership Map

This map defines primary ownership for high-impact validation invariants.
Higher-level layers may compose owners, but must not duplicate ownership logic.

| Invariant | Owner | Enforcement point | Should not live in | Tests |
| --- | --- | --- | --- | --- |
| DataFrame shape | `phospy.frames.validation` (`require_dataframe`, `require_non_empty_dataframe`, `require_aligned_dataframe_shape`); `phospy.validation.common.dataframes` is an identity-preserving compatibility route | Table schemas, dataset constructors, and workflow boundary validators compose shared frame primitives | Workflow scientific interpreters and scoring logic | `tests/unit/test_table_schemas.py`, `tests/unit/test_workflow_boundary_error.py` |
| Finite numeric intensity values | `phospy.science.tables.datasets.PhosphoIntensityMatrix` (composing `require_numeric_dataframe` + `require_finite_numeric_dataframe`) | Private construction service boundary: `_AnalysisReadyDatasetConstructionService._validate_analysis_ready_tables` composes `PhosphoIntensityMatrix` and `TotalProteinMatrix` | Public request dataclasses and workflow scoring stages | `tests/unit/test_table_schemas.py`, `tests/unit/test_public_contract_errors.py` |
| Analysis-ready quantitative numeric-domain semantics | `phospy.science.transformations.state_coherence.require_quantitative_numeric_domain_coherence` | Private construction service boundary after structural table wrappers and established `IntensityScaleState` validation | Table schemas, public request DTOs, workflow validators, column-name heuristics, and generic dataframe validators | `tests/unit/test_final_numeric_semantic_dataset_coherence.py` |
| Transformation scale/meaning model ownership | Policy enums and scale/meaning rules live in `phospy.science.transformations.policy`; provenance payload models live in `phospy.science.transformations.provenance`; immutable matrix/event value models live in `phospy.science.transformations.scale_values`; authority-gated state transitions live in `phospy.science.transformations.scale_state`. `phospy.science.transformations.models` is an identity-preserving import route only | Dataset builders, preprocessing stages, workflow quantitative validators, and bundle reconstruction compose the owned objects by identity | Duplicate compatibility classes, generic utility modules, and re-export routes with behavior | `tests/architecture/test_scientific_model_decomposition.py`, `tests/unit/test_science_package_ownership.py`, `tests/unit/test_transformation_provenance.py` |
| Unique site/sample labels | `phospy.frames.validation` (`require_unique_index`, `require_unique_columns`) composed by dataset table wrappers (`PhosphoIntensityMatrix`, `SampleMetadataTable`, `TotalProteinMatrix`) | Private construction service boundary: `_AnalysisReadyDatasetConstructionService._validate_analysis_ready_tables` composes dataset table wrappers | Workflow-local helper modules | `tests/unit/test_table_schemas.py`, `tests/unit/test_public_contract_dataset.py` |
| Strict phosphosite identifier format and site-ID/metadata coherence | `phospy.science.sites.validation` (strict site-id index/series checks + `require_site_identity_coherence`) | Dataset table schemas, prediction/activity/signalome table schemas, and workflow boundary contracts compose the science-owned checks | `phospy.validation.common.dataframes` | `tests/unit/test_site_validation_helpers.py`, `tests/unit/test_table_schemas.py`, `tests/unit/test_validator_boundaries.py` |
| Reusable phosphosite identity contracts | `phospy.science.sites.identity_rules.contracts` (`PhosphositeIdentityContract` and immutable policy records); `phospy.science.sites.identity_contracts` and `phospy.validation.identity_contracts` are identity-preserving compatibility import routes | Dataset table schemas, workflow identity composers, and result table constructors compose the science-owned contract objects and focused identity-rule modules | Independently defined validation-package contract classes, request DTOs, or facade-local implementations | `tests/architecture/test_validation_boundaries.py`, `tests/unit/validation/test_site_sequence_contracts.py`, `tests/validation/test_shared_identity_contracts.py`, `tests/unit/validation/test_phosphosite_identity_rule_families.py` |
| Analysis-ready `site_key` row identity | `phospy.science.sites.identity_rules.dataset_identity` (`enforce_analysis_ready_site_key_index`, `enforce_site_key_matches_metadata`, and related site-key identity checks); `phospy.validation.datasets.protein_scoped_site_identity` is an identity-preserving compatibility route | Private construction service boundary: `_AnalysisReadyDatasetConstructionService._validate_analysis_ready_tables`; builder must derive `site_key` before final construction, workflows compose but do not repair it | Workflow executors, reference mappers, result assemblers, and display-ID-only identity rules | `tests/unit/test_site_identity_validation.py`, `tests/integration/test_dataset_builder_boundary_honesty_integration.py`, `tests/unit/test_workflow_identity_contracts.py`, `tests/unit/validation/test_phosphosite_identity_rule_families.py` |
| Required site metadata | `phospy.science.tables.datasets.SiteMetadataTable` (composing `enforce_phosphosite_identity_contract` plus required non-empty identity columns) | Private construction service boundary: `_AnalysisReadyDatasetConstructionService._validate_analysis_ready_tables` composes `SiteMetadataTable` | Signalome/Kinase workflow validators as a primary owner | `tests/unit/test_table_schemas.py`, `tests/unit/test_public_contract_dataset.py` |
| Analysis-ready `site_sequence` evidence | `phospy.science.sites.metadata_validation.validate_site_sequence_column` composed by `phospy.science.tables.datasets.SiteMetadataTable`; `phospy.validation.datasets.site_metadata` re-exports the helper for internal compatibility | Private construction service boundary: `_AnalysisReadyDatasetConstructionService._validate_analysis_ready_tables`; preprocessing must derive-or-fail before construction; workflow validators compose only workflow-specific sequence-context requirements | Workflow validators as the primary owner of base dataset evidence; downstream scoring/interpreters | `tests/unit/test_table_schemas.py`, `tests/integration/test_dataset_builder_boundary_honesty_integration.py` |
| Workflow-specific centered sequence context | `phospy.science.sites.sequence_context` (`SequenceContextContract`, centered context enforcement, residue/window policy, and known sequence-source checks); validation/workflow modules compose or re-export it | Workflow boundary or resolved workflow validation before sequence-aware execution | Analysis-ready dataset private construction and public request DTOs | `tests/unit/validation/test_site_sequence_contracts.py`, `tests/unit/workflows/kinase/test_kinase_sequence_contract.py`, `tests/unit/workflows/signalome/test_signalome_sequence_contract.py` |
| Intensity scale establishment | `phospy.validation.transformations.state.IntensityScaleStateValidator` | `IntensityScaleStateValidator.run` composed by `_AnalysisReadyDatasetConstructionService._validate_analysis_ready_tables`, plus builder transformation establishment | Generic dataframe validators and workflow request dataclasses | `tests/unit/test_validation_ownership.py`, `tests/integration/test_dataset_builder_integration.py::test_dataset_builder_establishes_intensity_scale_state_via_supported_path` |
| Additive preprocessing scale eligibility | `phospy.science.datasets.preprocessing.quantitative_scale_policy.AdditivePreprocessingScaleGuard` | Private dataset builder execution before preprocessing starts; `DatasetBuildExecutor` composes the guard for median centring, fixed-effect residualisation, native SPS/RUV-style residualisation, and external additive corrected outputs | Differential, kinase, and Signalome workflow validators; public request DTOs; additive operation executors as independent policy owners | `tests/unit/test_additive_preprocessing_scale_policy.py`, `tests/architecture/test_validation_boundaries.py::test_additive_preprocessing_scale_policy_is_private_builder_owned` |
| Preprocessing plan stage order, imputation scale/order policy, and internal plan coherence | `phospy.science.datasets.preprocessing.plan_stage_order` owns stage-order validation/planning; `phospy.science.datasets.preprocessing.imputation_scale_policy` owns missing-data imputation scale/order resolution; `phospy.science.datasets.preprocessing.plan_rules` owns internal `PreprocessingPlan` field-family validation; `phospy.science.datasets.preprocessing.plan_config_resolution` owns public config policy resolution | `PreprocessingPlanInterpreter.run(...)` resolves public config into structured stage-order findings and imputation scale/order policy; `PreprocessingPlan.__post_init__` composes plan rule families before stages execute | Differential, kinase, Signalome, enrichment workflow validators; preprocessing stage executors; arbitrary dictionaries in place of typed plan/rule findings | `tests/unit/test_preprocessing_plan_interpreter.py`, `tests/unit/test_dataset_preprocessing_collaborators.py`, `tests/architecture/test_validation_componentization.py` |
| Processing state coherence | Processing-state dataclasses (`MissingDataState`, `NormalisationState`, `SiteSequenceResolutionState`, `TotalProteinCorrectionState`) for nested invariants; `_AnalysisReadyDatasetConstructionService._validate_analysis_ready_tables` for matrix-dependent coherence | State construction and private construction service boundary: `_AnalysisReadyDatasetConstructionService._validate_analysis_ready_tables` (`processing_state` type/coherence checks) | Workflow validators, executors, and table wrappers | `tests/unit/science/datasets/test_processing_state.py`, `tests/integration/test_dataset_builder_integration.py` |
| Duplicate `site_key` row resolution policy | Dataset preprocessing site-matrix stage configured by `DatasetSiteMatrixConfig.duplicate_site_policy` | Builder preprocessing before final `AnalysisReadyPhosphoDataset` construction; default policy raises on duplicate `site_key` rows | Workflow validators and downstream executors | `tests/integration/test_dataset_builder_integration.py`, `tests/integration/test_dataset_builder_boundary_honesty_integration.py` |
| Design matrix validity | `phospy.validation.workflows.differential.ExperimentalDesignContractValidator` coordinates `phospy.validation.workflows.differential_design_rules` rule families. `ResolvedDifferentialDesignMatrixValidator` consumes the shared immutable `phospy.science.differential.linear_model.DifferentialDesignDecomposition` for rank, conditioning, covariance, and DoF admissibility | Differential workflow validator/interpreter pipeline before execution; downstream eligibility, fitting, provenance, and diagnostics receive the same decomposition object by identity | Dataset schemas, generic `validation/common`, executor-side recomputation, and independent workflow-stage rank/conditioning calculations | `tests/unit/test_experimental_design_contract.py`, `tests/unit/test_differential_workflow_components.py`, `tests/unit/test_differential_analysis.py`, `tests/unit/test_differential_numerical_guards.py`, `tests/unit/test_workflow_stage_decomposition.py`, `tests/architecture/test_validation_componentization.py` |
| Contrast validity | `ExperimentalDesignContractValidator` coordinates `ExperimentalDesignContrastSetValidator`, `ExperimentalDesignConditionReplicateValidator`, `FixedBlockDesignValidator`, and `ContrastFrameBuilder`; the shared immutable scaled-SVD decomposition contract owns contrast covariance/estimability | Differential workflow validator/interpreter before executor; contrast covariance and diagnostics are derived from the same fitted decomposition object | Dataset constructors, table schemas, normal-equation contrast covariance calculations, and provenance assembled from a different design object | `tests/unit/test_experimental_design_contract.py`, `tests/unit/test_differential_workflow_components.py`, `tests/unit/test_differential_analysis.py`, `tests/unit/test_differential_numerical_guards.py`, `tests/unit/test_workflow_stage_decomposition.py`, `tests/architecture/test_validation_componentization.py` |
| Replicate policy | `phospy.workflows.differential.reliability` resolves production versus exploratory reliability profile; `phospy.validation.workflows.differential_design_rules.ExperimentalDesignConditionReplicateValidator` owns biological-replicate counting for contrasted conditions; `phospy.workflows.differential.replicates.TechnicalReplicateAggregationPlanner` owns validation-time technical-replicate policy checks and explicit plan construction; `phospy.workflows.differential.replicates.TechnicalReplicateAggregator` applies validated plans | `DifferentialAnalysisWorkflow.run(...)` composes the validator/interpreter pipeline: reliability policy resolves the biological-replicate threshold, the experimental-design validator enforces unique biological-replicate counts before design-matrix assembly, the validator builds a `TechnicalReplicateAggregationPlan`, and the interpreter applies that plan before execution when aggregation is required | Dataset table wrappers, shared generic validators, numerical executors, low-level linear-model fitting, and direct user calls to internal replicate helpers | `tests/integration/test_differential_with_technical_replicates.py`, `tests/unit/test_differential_technical_replicate_policy.py`, `tests/unit/test_differential_replicate_reliability_policy.py`, `tests/unit/test_validator_boundaries.py` |
| Public differential result identity | `phospy.science.differential.models.DifferentialAnalysisResult` result-table validation composing `phospy.science.sites.identity_rules.result_identity`; workflow executor only attaches dataset identity before constructing it | Public `DifferentialAnalysisResult` construction and `DifferentialAnalysisWorkflow.run(...)` output assembly | Lower-level stat-only differential computation payloads and workflow-local duplicate identity validators | `tests/unit/test_differential_result_contract.py`, `tests/workflows/test_identity_coherence.py`, `tests/unit/validation/test_phosphosite_identity_rule_families.py` |
| Scientific result caveat exposure | Workflow result assemblers and caveat builders (`phospy.workflows.differential.caveats`, `phospy.workflows.kinase.caveats`, `phospy.workflows.signalome.caveats`, `phospy.workflows.enrichment.caveats`) consume structured validator/interpreter facts; `phospy.contracts.result_caveats.ResultCaveat` owns the common payload shape | Result assembly before public result construction; public result constructors validate top-level `caveats` fields | Validators/interpreters as final user-facing message renderers, logs-only warnings, and provenance payloads as the only caveat surface | `tests/unit/test_result_caveats.py`, `tests/unit/test_differential_result_caveats.py`, `tests/unit/workflows/kinase/test_kinase_attrition_policy.py`, `tests/unit/test_signalome_workflow_components.py`, `tests/unit/test_enrichment_result_caveats.py` |
| Kinase reference compatibility | `phospy.validation.references.compatibility.ReferenceCompatibilityValidator` | Reference resolution/runtime boundary (resolver/workflow entry) | Kinase workflow scientific execution stages | `tests/unit/test_validation_ownership.py`, `tests/unit/test_validator_boundaries.py` |
| Reference bundle structural contract | `phospy.science.references.models.ReferenceBundleValidator` | `ReferenceBundle.__post_init__`; `phospy.validation.references.bundle` is a compatibility import route | Reference resolver and workflow execution stages | `tests/unit/test_reference_bundle_validation_report.py`, `tests/unit/test_validation_ownership.py` |
| Reference manifest value-model ownership | Manifest policy enums live in `phospy.science.references.manifest_policy`; file/window metadata lives in `phospy.science.references.manifest_files`; redistribution evidence lives in `phospy.science.references.redistribution`; aggregate manifest compatibility aliases live in `phospy.science.references.manifest_model`. `phospy.science.references.manifest` is an identity-preserving import route only, while JSON parsing remains in `phospy.science.references.validation.manifest_schema` | Reference bundle validation and release gates parse payloads into the owned manifest objects before semantic validation | Duplicate manifest dataclasses, parser-local model copies, and generic utility ownership of redistribution invariants | `tests/architecture/test_scientific_model_decomposition.py`, `tests/unit/references/test_reference_manifest.py`, `tests/release/test_reference_manifest_gate.py` |
| Kinase Library resource structural contract | `phospy.science.references.kinase_library.KinaseLibraryResourceValidator` | `KinaseLibraryResource.__post_init__`; `phospy.validation.references.kinase_library` is a compatibility import route | Workflow scoring/prediction execution stages | `tests/unit/test_kinase_library_references.py`, `tests/architecture/test_package_dependency_dag.py` |
| Kinase method quantitative input contracts | Individual method owners: scoring modes in `phospy.workflows.kinase.scoring_mode_contracts`, activity methods in `phospy.science.activities.method_contracts`; shared primitive `phospy.validation.workflows.method_quantitative.MethodQuantitativeInputValidator` only enforces a supplied contract | `KinaseWorkflowValidator` and `KinaseWorkflowInterpreter` compose the selected method contract before execution; activity methods also guard typed activity-input semantics at their own boundary | `WorkflowQuantitativeInputValidator`, public request DTOs, scoring/activity executors that silently relabel or transform values, and any single global kinase scale rule | `tests/unit/workflows/kinase/test_kinase_method_quantitative_contracts.py`, `tests/architecture/test_validation_boundaries.py` |
| Kinase post-resolution scientific eligibility | `phospy.workflows.kinase.resolved_validator.ResolvedKinaseEligibilityValidator` | Kinase interpreter after reference resolution/projection, sequence-support merge, and execution-config resolution; before executor receives `ResolvedKinaseWorkflowRequest` | `KinaseWorkflowExecutor`, scoring/prediction/activity runners, and initial `KinaseWorkflowValidator` checks that only have pre-interpretation inputs | `tests/unit/workflows/kinase/test_resolved_kinase_validator.py`, `tests/unit/test_workflow_boundary_error.py` |
| Signalome protein grouping metadata | `phospy.validation.datasets.site_metadata.enforce_required_non_empty_string_column` | Signalome workflow validator (`SignalomeWorkflowValidator`) | Dataset row identity validators and signalome science modules | `tests/unit/test_validator_boundaries.py::test_signalome_validator_requires_signalome_protein_grouping_metadata_column`, `tests/unit/test_workflow_boundary_error.py` |
| Localisation eligibility | `phospy.validation.datasets.site_metadata.enforce_localisation_requirement` | Workflow validators (`KinaseWorkflowValidator`, `SignalomeWorkflowValidator`) compose the owner with request-configured policy | Dataset structural wrappers and reference validators | `tests/unit/test_validator_boundaries.py`, `tests/integration/test_localisation_policy_integration.py` |
| Peptide evidence ambiguity | `phospy.science.evidence.multi_site.resolve_observation_site_rows` + `phospy.science.evidence.dataset_resolution.PeptideEvidenceDatasetResolver` (+ request mode/policy gate in `DatasetBuildRequestValidator`) | Dataset build preprocessing lane for `site_resolution_mode='peptide_evidence'` | Table schemas for analysis-ready dataset and downstream workflow validators | `tests/unit/test_dataset_peptide_evidence_resolution.py`, `tests/unit/test_evidence_multi_site_handling.py` |

## Ownership Notes

- Dataset validation remains private and construction-bound: there is no public
  dataset `validate()` API.
- Public constructor validation is explicit:
  - Passive request DTOs (`DatasetBuildRequest`, `PhosphositeImportRequest`,
    `DifferentialAnalysisRequest`, `EnrichmentWorkflowRequest`,
    `KinaseWorkflowRequest`, and `SignalomeWorkflowRequest`) store payloads only.
    Request type compatibility, contextual compatibility, and scientific
    readiness are validated by the owning builder or workflow validator.
  - Generic public value objects in `phospy.contracts` and public
    design/enrichment contract objects validate only local, context-free
    scalar/container invariants during construction and raise
    `ContractValidationError`.
  - Dataset constructors raise dataset-boundary validation errors
    (`DatasetValidationError`) for strict analysis-ready dataset invariants.
  - Reference bundle/resource constructors raise reference-boundary validation
    errors (`ReferenceValidationError`) for reference structural invariants.
  - Dataset-build preprocessing config constructors are builder-input policy
    value objects. They validate local preprocessing option shape and continue
    to raise `PhosPyInputError`, while data-dependent preprocessing eligibility
    remains in the builder validation path.
- Users validate dataset inputs by constructing datasets through supported
  builders, or by running workflows that validate requests at their boundary.
  Direct imports or calls into `phospy.validation.datasets` are internal support
  paths for package code and tests, not supported user entrypoints.
- Validation-domain adapters may call dataset construction, but they must not
  reimplement `AnalysisReadyPhosphoDataset` invariants.
- Validation-domain compatibility routes may re-export science-owned
  model-boundary validators, but value objects must not import validation
  modules to validate themselves.
- Validation-domain identity and sequence-context compatibility routes must
  preserve object identity with the science-owned contract classes and
  enforcement functions.
- Workflow validators should compose shared/domain validators instead of duplicating row-level scientific identity checks.
- Initial workflow validators own request, dataset, and configuration invariants
  available before interpretation. Resolved validators own scientific
  eligibility checks that require interpreted references, configuration, or
  executable inputs. Executors do not become validation owners beyond defensive
  internal checks.
- Scientific caveats follow
  [ADR-0033](adr/adr_0033_result_caveats.md): validators and interpreters
  produce structured facts, result assemblers attach user-facing top-level
  `caveats`, and severe caveats must not be log-only.
- Caveats complement provenance. They should summarize interpretation risks,
  limitations, assumptions, and policy overrides without duplicating full
  provenance payloads or becoming arbitrary message containers.
- Public request dataclasses remain transport objects; boundary ownership sits in validators and domain contracts.
- Differential replicate policy is owned by the public
  `DifferentialAnalysisWorkflow.run(...)` pipeline through reliability,
  experimental-design, planner, and aggregator collaborators.
  `TechnicalReplicateResolver` remains an internal backward-compatible wrapper
  around those collaborators, not the current ownership or normal user
  entrypoint. Numerical executors must receive already-validated matrices and
  must not reinterpret technical or biological replicate semantics.
- Policy enums are owned by behavioural domain modules:
  - preprocessing: `phospy.science.datasets.preprocessing.policy_models`
  - differential: `phospy.science.differential.policy_models`
  - shared scoring: `phospy.science.scoring.policy_models`
  - shared policy-enum infrastructure: `phospy.policies.policy_base`
  - root module `phospy.policy_models` has been removed.
- Scientific table schemas are owned under `phospy.science.tables`. Generic
  table-schema infrastructure (`TableSchema`, `ValidationErrorType`, and the
  label-index helper) is owned under `phospy.frames`. The
  `phospy.tables` package is retained only as an identity-preserving
  compatibility import layer for supported table routes.
