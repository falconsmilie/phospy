# Kinase Workflow

The detailed kinase workflow API page has moved to
[Kinase Workflow](workflows/kinase.md).

Use `KinaseWorkflow` from top-level `phospy`, and use `KinaseWorkflowRequest`,
`KinaseScoringConfig`, `KinasePredictionConfig`, `KinaseActivityConfig`,
`ReferencePreset`, and `ReferenceBundle` from `phospy.api`.
