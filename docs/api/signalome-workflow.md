# Signalome Workflow

The detailed signalome workflow API page has moved to
[Signalome Workflow](workflows/signalome.md).

Use `SignalomeWorkflow` from top-level `phospy`, and use
`SignalomeWorkflowRequest` and `SignalomeConfig` from `phospy.api`.
